<!DOCTYPE html>
<html>
  <head>
    <title>yield(:title)</title>
    <%=render 'layouts/default' %>
  </head>
    
  <body>
    <div class="container">
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
